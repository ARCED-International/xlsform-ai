{
  "name": "xlsform-ai-project",
  "version": "0.5.0",
  "description": "XLSForm project with AI-assisted development",
  "scripts": {
    "watch": "nodemon --watch survey.xlsx --exec \"npm run upload && npm run reload\"",
    "upload": "python scripts/upload_to_central.py",
    "reload": "node scripts/reload_browser.js",
    "validate": "python scripts/validate_form.py survey.xlsx"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "keywords": ["xlsform", "odk", "survey"],
  "author": "ARCED International",
  "license": "MIT"
}
